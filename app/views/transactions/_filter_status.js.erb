$("#trans_list").replaceWith('<%= escape_javascript(render('transactions/trans_list')).html_safe %>');
$("#all-button").remove();
<% if params[:status] != "all" %>
  $(".filter-transaction").append('<%= link_to "Всі", filter_status_path(status: "all"), id: "all-button", remote: true %>');
<% end %>
